{#
/**
 * @file
 * Template for a generic layout.
 */
#}
{%
  set classes = [
    'layout',
    'layout--' ~ layout.id|clean_class,
  ]
%}
{% if content %}
    {% for region in layout.getRegionNames %}
      {% if content[region] %}
          {{ content[region] }}
      {% endif %}
    {% endfor %}
{% endif %}
